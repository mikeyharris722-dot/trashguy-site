Replace these two files:

app/page.tsx
components/site-header.tsx

Then run:

npm run build

This fixes the top login area so Twitch and Kick are shown as separate buttons. It also adds both buttons inside the Bonus Hunt prediction sign-in area.
